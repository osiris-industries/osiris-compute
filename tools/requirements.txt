# Build/partition toolchain deps (Python 3.10+). torch CPU wheel is fine:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
torch
transformers
tokenizers
onnx
onnxruntime
onnxscript
safetensors
huggingface_hub
numpy
